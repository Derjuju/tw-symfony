<div class="row">
    <div class="col-lg-12">
        {% if entity.id is not null %}
            {% if entity is instance_of_addressrdv %}
                <h2 class="page-header">Adresses Utilisateurs</h2>
            {% else %}
                <h2 class="page-header">Adresses TW</h2>
            {% endif %}
        {% else %}
        <h2 class="page-header">Nouvelle adresse TW</h2>
        {% endif %}
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
    